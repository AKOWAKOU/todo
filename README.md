## SETUP
git clone git@github.com:AKOWAKOU/todo.git &&
cd todo
## INSTALLATION
npm install && npm install axios cors 
## build app

docker-compose up --build

## UI IN APPLICATION
http://localhost:3000
